# 📌 دليل أوامر مشروع Dalil (عبر SSH)
---
**الكلمة المفتاحية:** `dalil`

### 1. تشغيل (نظيف):
> `dalil start`
*(يقوم بمسح العمليات القديمة وتشغيل السيرفر على بورت 3000)*

### 2. إيقاف السيرفر:
> `dalil stop`

### 3. إعادة التشغيل:
> `dalil restart`

### 4. أخذ نسخة احتياطية (Zip):
> `dalil backup`
*(المكان: `~/backups_dalil`)*

### 5. تنظيف الـ Logs والمساحة:
> `dalil clean`

### 6. فحص استهلاك السيرفر:
> `dalil status`

---
### 💡 نصيحة مهندس:
بعد تشغيل السيرفر لأول مرة، اكتب الأوامر التالية لضمان التشغيل التلقائي حتى بعد ريستارت السيرفر:
`pm2 save && pm2 startup`
